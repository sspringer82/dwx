{
  "name": "07-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Node test runner + assert + spies + module mocks demo",
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-test-module-mocks",
    "test:watch": "node --test --watch --experimental-test-module-mocks"
  },
  "dependencies": {
    "axios": "^1.18.1"
  }
}
